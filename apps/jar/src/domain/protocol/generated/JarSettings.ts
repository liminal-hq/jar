// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { DialogTheme } from "./DialogTheme";
import type { Habitat } from "./Habitat";
import type { LightColour } from "./LightColour";
import type { TankFrame } from "./TankFrame";

export type JarSettings = { habitat: Habitat, frame: TankFrame, dialog_theme: DialogTheme, 
/**
 * One remembered variant name per theme (SPEC.md §4's "Variants" table
 * and §6's "dialog theme + variant per theme" persistence rule) — kept
 * as free strings here since the variant vocabulary differs per theme
 * and is a presentation-only concern the core doesn't interpret.
 * Switching `dialog_theme` never touches this map; only an explicit
 * variant pick (or the initial default below) writes to it.
 */
theme_variants: { [key in DialogTheme]?: string }, light_on: boolean, light_colour: LightColour, 
/**
 * Percentage scale (0-200, 100 = the fixture's own designed default)
 * applied to the castle's ground-level uplight (`Castle.tsx`'s
 * `GroundUplight`) — a separate knob from `light_on`/`light_colour`,
 * which drive the tank-wide LED strip; this one only tunes the
 * castle's own always-on accent light.
 */
light_intensity: number, ambient_particles_on: boolean, 
/**
 * Percentage scale (0-200, 100 = the current default bubble count)
 * applied to `Bubbles.tsx`'s particle count — a separate knob from
 * `ambient_particles_on`, which is the on/off toggle this only takes
 * effect underneath.
 */
bubble_intensity: number, sound_on: boolean, 
/**
 * 1-60, Real time = 1 (SPEC.md §5).
 */
simulation_speed: number, always_on_top: boolean, };
