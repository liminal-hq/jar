[package]
name = "jar-core"
version = "0.1.0"
description = "Pure simulation core for Jar's critters — aging, mood/energy, breeding, genetics, passing, naming, jar clock"
edition.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
publish = false

[dependencies]
jar-protocol = { path = "../jar-protocol" }
serde = { workspace = true }
postcard = { workspace = true }
thiserror = { workspace = true }
rand = { workspace = true }
