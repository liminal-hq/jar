# Plugin docs

Jar currently has one plugin, `plugins/tauri-plugin-jar`, documented in `docs/architecture/rust-core.md`. This directory is for future plugin-specific docs (manifest/permission conventions, a command reference per plugin) once there's more than one plugin to distinguish between — mirroring the `docs/plugins/` convention from Liminal HQ's other Tauri apps.
